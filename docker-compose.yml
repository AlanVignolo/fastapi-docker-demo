services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    environment:
      - ENV=development
  
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"